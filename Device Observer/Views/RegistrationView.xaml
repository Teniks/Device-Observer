<Page x:Class="Device_Observer.Views.RegistrationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Device_Observer.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RegistrationView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="6*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel MaxHeight="230" MaxWidth="300">
            <TextBlock Text="Login:" Margin="10,2"  Foreground="White"/>
            <TextBox x:Name="LoginBox" Margin="10"/>
            <TextBlock Text="Password:" Margin="10,2"  Foreground="White"/>
            <TextBox x:Name="PasswordBox" Margin="10"/>
            <TextBlock Text="Details:" Margin="10,2" Foreground="White"/>
            <TextBox x:Name="DetailsBox" Margin="10"/>
            <Button x:Name="DetailsBtn" Click="RegistrationBtn_Click"  Content="Registration" Margin="10"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Button x:Name="GoBackBtn" Content="Вернуться" Click="GoBackBtn_Click"  Grid.Row="1" MaxHeight="40" Margin="15"/>
        </Grid>
    </Grid>
</Page>

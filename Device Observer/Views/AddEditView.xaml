<Page x:Class="Device_Observer.Views.AddEditView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Device_Observer.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddEditView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="6*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel MaxWidth="780" MaxHeight="400" Grid.Row="0">
            <TextBlock x:Name="firstBlock" Margin="10, 2"/>
            <TextBox x:Name="firstBox" Margin="10"/>
            <TextBlock x:Name="secondBlock" Margin="10, 2"/>
            <TextBox x:Name="secondBox" Margin="10"/>
            <TextBlock x:Name="thirdBlock" Margin="10, 2"/>
            <TextBox x:Name="thirdBox" Margin="10"/>
            <Button x:Name="Save" Click="Save_Click" Content="Сохранить" Margin="15, 20"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Button x:Name="GoBackBtn" Content="Вернуться" Click="GoBackBtn_Click" Grid.Row="1" MaxHeight="40" Margin="15"/>
        </Grid>
    </Grid>
</Page>
